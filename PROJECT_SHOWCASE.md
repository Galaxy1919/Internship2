# 项目家底 · 演示与答辩手册

> 东北大学软件学院《信息安全工程实训2》课程项目。
> 本文档是项目成果的完整盘点，以及现场演示与答辩的速查手册。

---

## 1. 一句话定位

一个**从零实现**的信息加解密实验系统：14 个密码算法（仅用标准库，不调任何第三方密码库）+ 双机安全信道 + 跨语言互操作 + 系统内智能编排 Agent，覆盖「单机算法 → 双机通信 → 安全攻防 → 智能应用」的完整链路。

---

## 2. 完整家底清单

### 2.1 密码算法（14 个，全部从零实现）

| 类别 | 算法 | 备注 |
|---|---|---|
| 古典 | 单表替代（Polybius 方阵） | |
| 古典 | 多表替代（Vigenère + Autokey） | 另有 C 独立实现 |
| 古典 | 列置换 | 另有 C 独立实现 |
| 古典 | Playfair（多图替代） | |
| 流密码 | RC4 | 与标准库交叉验证 |
| 流密码 | CA 元胞自动机（Rule 30） | 与标准库交叉验证 |
| 分组 | DES | 手写 S-box + Feistel |
| 分组 | AES-128 | 手写 S-box + 轮函数 |
| 公钥 | RSA | Miller-Rabin + CRT + 低指数攻击演示 |
| 公钥 | ECC（secp256k1） | 仅 ECDH，无消息加密 |
| 公钥 | ElGamal | 安全素数 + k 复用攻击演示 |
| 公钥 | SM2（国密） | 手写 SM3 + SM2-PKE + SM2-DSA，另有 C 独立实现 |
| 散列 | MD5 | RFC 1321 |

### 2.2 四种运行形态（同一套算法，不同载体）

| 形态 | 载体 | 说明 |
|---|---|---|
| 单机 | Web（Next.js/TS） | 14 个 lab，浏览器端独立实现 |
| 双机 | CLI（Python） | DH/socket，加密端 Alice ↔ 解密端 Bob |
| 双机 | Web（/dual） | spawn Python CLI 做后端，不重写算法 |
| 桌面端 | PyQt6 | 6 页面统一操作：总览/算法实验/双机信道/攻击实验/验证中心/智能助手 |

### 2.3 双机安全信道（技术核心）

- **DH 密钥交换** + KDF 派生传输密钥 + HMAC 完整性校验
- **动态传输密码**：aes/des/rc4/ca 可选，密钥由 DH 派生
- **认证 DH（防中间人）**：Bob 用长期 SM2 身份密钥对 DH 交换做签名，Alice 验签
- **重放保护**：每个 JSON 帧带递增 seq 序号，服务端拒绝重复/乱序帧
- **文件传输**：客户端流式读取，64KB 分块加密 + HMAC，服务端校验声明大小并使用临时文件原子落盘，末尾空帧做 EOF
- **公钥反向流**：RSA/ElGamal/SM2（解密端生成密钥对，公钥发给加密端）
- **MD5 摘要校验** + **ECDH 密钥交换**

### 2.4 跨语言（C ↔ Python）

- C 版 SM2（自研 256-bit 大整数库 `bn.c`）+ Autokey-plaintext + 双重置换
- SM2 Python↔C 密文互操作：双向加解密往返均通过，证明两种语言实现等价
- 基准：`sm2_cli bench` 区分 C 单进程循环与 Python 调 C CLI 的 subprocess 成本，`sm2_cli batch` 支持常驻进程批量调用

### 2.5 智能 Agent（系统内密码学应用编排器）

- 10 个密码原语 CLI（对称/公钥/SM2 签名验签/散列/HMAC），统一 JSON 输出
- function calling + ReAct 循环（think → act → observe）
- 三个旗舰应用：**混合加密（数字信封）** / **数字签名** / **加密保险箱**
- **PBKDF2 加盐密钥派生**（口令 → 密钥，防彩虹表）

### 2.6 安全 / 攻击实验

- **MITM 中间人攻击**：认证 DH 攻防对照（无认证被攻破 / 有认证被识破）
- **ElGamal k 复用攻击**：随机数复用 → r 相同 → 恢复私钥
- **RSA 低指数攻击**：代码内置演示

### 2.7 验证体系

| 验证 | 脚本 | 证明 |
|---|---|---|
| 官方向量 | `test_vectors.py` | AES/DES/SM3 与 NIST/GM/T 一致 |
| 标准库交叉验证 | `verify.py` | MD5/RC4/CA 与标准库一致 |
| 双机端到端 | `DH/test_integration.py` | 全帧类型往返一致 |
| 安全信道 | `DH/test_security.py` | 认证/MITM/重放全部拦截 |
| C 实现 | `c_verify.py` | SM2 跨语言互操作 + Autokey/双重置换 C 自检 |

---

## 3. 技术亮点（Top 3）

1. **智能 Agent 编排器** —— 把基础密码原语自主编排成高层应用（混合加密等），模型只调度工具、真正的计算由项目自己的代码完成。这是大多数同类项目没有的能力。
2. **安全信道攻防** —— 不是"能加密"就完事，而是补全了认证 DH（防中间人）+ 重放保护，并现场演示攻击与防御的对照，体现安全思维。
3. **跨语言互操作** —— C 与 Python 两套独立实现，密文互解、结果一致，是"算法实现正确"的客观证据，而不是自说自话。

---

## 4. 现场演示手册（按冲击力排序，答辩挑 3-4 个即可）

### 演示 1：智能 Agent 编排器（最大亮点）

**操作**：桌面端 `python3 desktop/run_desktop.py` → 左侧「智能助手」→ 右上角设置填 API（https://api.deepseek.com + deepseek-v4-flash + key）→ 点场景按钮「混合加密」。

**看什么**：对话区实时展示编排过程——生成会话密钥 → AES 加密正文 → 生成 RSA 密钥对 → 公钥加密会话密钥 → 数字信封组装 → 解密还原验证。

**讲什么**：
> 智能助手不是密码算法的替代实现，而是系统内的应用编排器。它调用项目自己的密码原语（封装成 CLI），把基础算法编排成混合加密这样的高层应用。模型只负责规划步骤和调度工具，真正的加密计算仍由项目代码执行。

### 演示 2：MITM 中间人攻防（视觉冲击最强）

**操作**（三个终端）：
```bash
# 终端 1：真实 Bob
cd DH && python3 decrypt_server.py --port 29090
# 终端 2：中间人 Eve
python3 mitm.py --mitm-port 39000 --target-port 29090
# 终端 3：Alice 连 Eve
python3 encrypt_client.py "绝密消息" --port 39000 --no-auth   # 攻击得逞
python3 encrypt_client.py "绝密消息" --port 39000             # 攻击被识破
```

**看什么**：加 `--no-auth` 时 Eve 终端打印「★ 攻击成功：解密得到明文」；去掉后 Alice 打印「检测到中间人攻击」并断开。

**讲什么**：
> 原始 DH 只协商共享秘密，不认证身份，中间人可以分别和双方各做一次 DH，把双方隔开。认证 DH 让 Bob 用长期 SM2 身份密钥对交换过程签名，Alice 验签，中间人无法伪造签名，替换公开值会立即被识破。

### 演示 3：双机文件传输 + 安全信道（完整链路）

**操作**：浏览器打开 http://localhost:3900/dual → 模式选「文件传输」→ 选一个文件 → 运行。

**看什么**：DH 共享密钥两端一致、文件大小两端一致、Bob 保存路径（DH/received/）。

**讲什么**：
> 双机不是把明文从一个窗口复制到另一个窗口。先 DH 密钥交换，再用共享秘密派生传输密钥和 HMAC 密钥，之后才发加密数据；解密端先校验完整性再解密。网络上传的永远是密文。

### 演示 4：C 跨语言互操作（技术深度）

**操作**：`cd publicKey/SM2 && python3 interop.py --selftest`

**看什么**：C 加密 → Python 解密、Python 加密 → C 解密，双向明文一致。

**讲什么**：
> SM2 我们用 C 和 Python 各写了一套独立实现。它们用不同的序列化格式，但数学上一致；桥接后密文能互相解开，这证明算法实现正确——不是靠"自己说自己对"，而是两套实现互为对照。

### 演示 5：ElGamal k 复用攻击（安全反面教材）

**操作**：桌面端「攻击实验」页。

**看什么**：复用随机数 k 导致两次签名 r 相同，攻击者据此恢复私钥。

**讲什么**：
> 算法本身正确，不代表用法正确。ElGamal 签名如果复用随机数 k，r 会相同，攻击者就能恢复私钥。修复方式是每次用新的不可预测的随机数。

---

## 5. 答辩追问弹药（Q&A）

| 追问 | 回答要点 |
|---|---|
| 为什么裸 RSA/ElGamal 只能加密短消息？ | 无填充 + 明文必须小于模数 n/p，只适合短消息；长消息用混合加密。 |
| 混合加密为什么是核心思想？ | 对称密码快、公钥密码解决密钥分发，各取所长；TLS/HTTPS/PGP 的底层都是它。 |
| 为什么口令派生密钥要用 PBKDF2？ | 慢哈希 + 随机盐，对抗彩虹表和暴力破解；直接散列口令是危险的。 |
| 认证 DH 为什么能防中间人？ | 签名绑定整个交换 transcript，中间人无法伪造 Bob 的签名，篡改公开值验签必失败。 |
| 为什么还实现 MD5？ | MD5 已不适合安全用途，但适合做单向散列的历史教学和雪崩效应实验。 |
| 重放保护怎么做？ | 每个帧带递增序号，接收方校验严格递增，重复帧被拒。 |
| 为什么 C CLI 路径可能比 Python 慢？ | CLI 基准包含 subprocess 启动、hex 转换和管道 I/O；Python 大整数本身由 CPython C 后端优化。项目已用 `sm2_cli bench` 单独统计 C 进程内循环耗时；当前 C 实现保留经过验证的仿射点乘。 |
| SM2 有什么特别？ | ZA 预处理把用户身份 ID 和公钥一起哈希进签名，绑定身份、防跨用户重放。 |
| 原始 DH 能不能防中间人？ | 不能。原始 DH 不认证身份；需要数字签名或证书（本项目用 SM2 签名做认证 DH）。 |

---

## 6. 一键验证（答辩兜底）

```bash
cd /Users/mac/code/Internship2
python3 test_vectors.py                       # 官方向量：AES/DES/SM3
cd DH && python3 test_integration.py          # 双机端到端：全帧类型
python3 test_security.py                      # 安全信道：认证/MITM/重放
cd .. && python3 c_verify.py                  # C 实现：SM2 互操作 + Autokey/双重置换
python3 run_tests.py                          # 以上项目验收的统一入口
```

全部 PASS 即证明：算法与标准一致、双机链路正确、安全防御有效、C 实现可编译并通过互操作/自检——一句话说完整个项目。
